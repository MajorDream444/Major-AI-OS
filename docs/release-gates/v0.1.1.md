# MAIM Release Gate - v0.1.1

Status: merged, tag pending
Release date: 2026-07-07
Primary system: MAIM ABC canon and landing-page content consistency

## What Changed

- Locked the canonical ABC spine to:

```txt
Awareness
Belief
Context
Direction
Experiment
```

- Updated `E = Experiment` in the canonical ABC database.
- Updated the landing-page CMS fallback so the visible A-E roadmap shows `Experiment`.
- Documented the human-readable canon in `docs/abc-framework.md`.
- Preserved the distinction:

```txt
E = Experiment
X = eXecution
```

## What Was Tested

Level 1 - Code Quality:

- JSON/content changes were reviewed for consistency.
- Landing CMS fallback was checked for the updated E record.

Level 2 - Logic:

- Canonical data, docs, and landing fallback now agree on `Experiment`.
- `Experiment` is treated as the testing layer.
- `eXecution` remains the shipping layer.

Level 3 - Platform:

- Production CMS smoke check confirmed the live content showed:

```txt
E=Experiment | Test before you systemize.
```

Level 4 - Production:

- Main branch merged.
- Production redeploy completed after the canon update.

## What Is Still Disabled

- Airtable sync.
- Live welcome email.
- Calendar invite.
- ManyChat.
- CRM write.

## What Is Safe

- ABC data model now has a clean A-E foundation.
- Landing-page A-E copy no longer conflicts with the canonical system.
- Future content and agent routing can build on `Experiment` without spreading the old inconsistency.

## What Is Experimental

- Full ABC database activation beyond the A-E public introduction.
- Agent-specific lesson routing by ABC letter.
- CMS-managed ABC editing workflow.

## Rollback Procedure

1. Revert PR #17 if the canon change needs to be backed out.
2. Redeploy the previous production build.
3. Re-check the landing page fallback CMS JSON for the E record.

## Production URL

Current Vercel deployment. Exact URL was not recorded in the repo at this gate.

## Git Tag

```txt
Proposed: v0.1.1
Status: not created yet
```

## PR Numbers

```txt
PR #17 - Canonical E equals Experiment update
```

## Evidence

```txt
9b879df - Merge pull request #17
```
